<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- AR Core availability check (optional install) -->
    <uses-feature android:name="android.hardware.camera.ar" android:required="false" />

    <!-- Required for Android 11+ to detect and communicate with wallet apps -->
    <queries>
        <!-- MetaMask -->
        <package android:name="io.metamask" />
        <!-- Trust Wallet -->
        <package android:name="com.wallet.crypto.trustapp" />
        <!-- Rainbow -->
        <package android:name="me.rainbow" />
        <!-- Coinbase Wallet -->
        <package android:name="org.toshi" />
        <!-- Google Scene Viewer for AR -->
        <package android:name="com.google.android.googlequicksearchbox" />
        <package android:name="com.google.ar.core" />
        <!-- Generic WalletConnect URI support -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="wc" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="metamask" />
        </intent>
    </queries>

    <application
        android:name=".ChainTorqueApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChainTorqueNative"
        tools:replace="android:dataExtractionRules,android:fullBackupContent">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:hardwareAccelerated="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Deep link for WalletConnect callbacks -->

        </activity>
    </application>

</manifest>
